[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP Server with JWT token validation"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.6.0",
    "python-jose[cryptography]>=3.3.0",
    "httpx>=0.28.0",
    "pydantic>=2.10.0",
]
